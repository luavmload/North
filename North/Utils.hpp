#pragma once

#include <zstd.h>

namespace Utils
{
	inline std::string CompressBytecode(std::string source)
	{
		// Compress bytecode with zstd and sign with rsb1 
		std::string compressed = "";
		return compressed;
	}
};